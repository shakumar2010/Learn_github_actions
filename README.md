# Learn_github_actions

This project is set up with Gradle (Java 17).

## Run locally

- Install Java 17
- Install Gradle (if wrapper is not present)
- Run:
  - `gradle clean build`

## CI

GitHub Actions workflow at `.github/workflows/hello.yaml` builds and tests the project on push and pull requests.
